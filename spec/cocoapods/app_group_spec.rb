require 'spec_helper'

describe Cocoapods::AppGroup do
  it 'has a version number' do
    expect(Cocoapods::AppGroup::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
