module Pod
  class Command
    class AppGroup < Command
      self.summary = 'Setup app group'
      self.description = 'Setup app group'

      def run
        # DO SOMETHING
      end
    end
  end
end
